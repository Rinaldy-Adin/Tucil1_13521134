#include <string>
#include <vector>

using std::string;
using std::vector;

vector<string> evaluateExpressions(int permutations[][4], int nPerms, char possibleExpressions[][7],
                                   int nExps);